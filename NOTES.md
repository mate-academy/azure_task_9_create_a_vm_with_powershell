The VM was initially planned to be deployed in the `uksouth` region with the `Standard_B1s` size.

However, the required VM size was not available in the `uksouth` region for my subscription. Therefore, I deployed the VM in the `northcentralus` region and used the `Standard_B2ats_v2` size instead.

The VM and all required resources were successfully deployed and are working correctly.